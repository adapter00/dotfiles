#!/bin/sh


if  'gui_macvim '
    set showtableline 2	'タブを常に設定'
	set imdisable		'IMを無効化'
	set syn=evening
 	set ts=1
	set transparency=5
    set gfn = Ricty\ Regular:h16

endif

if	'gui_runnnig'
	set
	au GUIEnter set
endif
